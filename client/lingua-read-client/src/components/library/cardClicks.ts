// A click on a Library card opens the item, except when it lands on one of the card's own
// controls: links and buttons, the selection checkbox, or the drag handle.
export const CARD_INTERACTIVE_SELECTOR = 'a, button, input, label, .form-check, .dropdown, [data-drag-handle]';
